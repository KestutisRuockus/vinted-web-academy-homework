import { ActivePage } from "../types/types";

export const navLinks: { name: string; component: ActivePage }[] = [
  { name: "Home", component: "photos" },
  { name: "Favourite", component: "favourite" },
];
